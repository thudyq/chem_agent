# 氢键绘制规范

氢键形成于 `X—H···Y` 三原子体系：共价键 `X—H` 用实线绘制（渲染器会显式画出给体的 H 并带 H 标签），氢键 `H···Y` 用点状虚线绘制（3~10 个点），三原子尽可能呈直线排列（分子内氢键可例外）。

你只需在 `[HBOND]` 标记中正确指出给体原子（from，X）与受体原子（to，Y），其余画法（构象调整、点数、避让标签）由渲染器自动处理。标记语法见主提示第 11 条。
