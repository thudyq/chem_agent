[pytest]
# 测试基临时目录由 tests/conftest.py 的 `tmp_path` fixture 提供（带 TMPDIR
# 回退）：pytest 默认把临时目录放系统 temp，而受限环境（容器/CI/DSH 沙箱）
# 常禁止写系统 temp —— 那会让 tmp_path 与依赖 TemporaryDirectory 的用例以
# WinError 5 / PermissionError 报错，被误读成"代码坏了"。
# 需要指到别处时设环境变量 CHEM_AGENT_TEST_TMP=<目录>。
testpaths = tests
